
JAVA学习笔记

//11月16日更新

类名：对于所有的类来说，类名的首字母应该大写。如果类名由若干单词组成，那么每个单词的首字母应该大写，例如 MyFirstJavaClass 
方法名：所有的方法名都应该以小写字母开头。如果方法名含有若干单词，则后面的每个单词首字母大写

final	表示一个值在初始化之后就不能再改变了      表示方法不能被重写，或者一个类不能有子类
finally	为了完成执行的代码而设计的，主要是为了程序的健壮性和完整性，无论有没有异常发生都执行代码。

定义 float 类型时必须在数字后面跟上 F 或者 f

重写or重载 
重载(overloading) 是在一个类里面，方法名字相同，而参数不同。返回类型可以相同也可以不同。

每个重载的方法（或者构造函数）都必须有一个独一无二的参数类型列表。

最常用的地方就是构造器的重载。

重载规则

被重载的方法必须改变参数列表(参数个数或类型或顺序不一样)；
被重载的方法可以改变返回类型；
被重载的方法可以改变访问修饰符；
被重载的方法可以声明新的或更广的检查异常；
方法能够在同一个类中或者在一个子类中被重载。
无法以返回值类型作为重载函数的区分标准。


ArrayList的用法
http://blog.csdn.net/lmn669/article/details/77929660?locationNum=1&fps=1


//11月25日更新
JAVA是单继承的，一个子类的父类只有一个的说法是错误的。（Java不是单继承）

子类的构造函数一定会调用父类的构造函数

一个类可以有多个构造函数，参数不同就可以。

使用super（）引用父类的构造函数时一定要放在本类构造函数的第一句。


//11月27日更新

static的用法，老师PDF里面

接口的很多东西没弄懂 
比如   以接口名为声明类型的变量，可以被实现了该接口的任一类的对象赋值

JAVA的新特性：接口里面的方法也可以写内容，只要在方法名前面写上default就OK

is-a 继承    has-a 聚合    like-a  组合   三种不同的关系


//11月28日更新

包名小写、类名大写（文件名与类名相同）、方法名小写、

方法的重写（覆盖）、重载，一个垂直关系，另外一个水平关系



